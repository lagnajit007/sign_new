[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.env]
FRONTEND_ORIGIN = "https://sanjog.vercel.app"
RATELIMIT_PER_MINUTE = "60"
